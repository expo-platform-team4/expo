<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expo.booth.repository.ClientDashboardBoothMapper">

    <select id="findByClientUserId" resultType="com.expo.booth.dto.ClientDashboardBoothResponse">
        SELECT client_user_id,
               recruitment_notice_id,
               application_id,
               application_status,
               booth_order_id,
               booth_order_status,
               payment_status,
               paid_at,
               booth_allocation_id,
               allocation_status,
               booth_number
        FROM v_client_dashboard_booths
        WHERE client_user_id = #{clientUserId}
        ORDER BY application_id DESC
    </select>

</mapper>
