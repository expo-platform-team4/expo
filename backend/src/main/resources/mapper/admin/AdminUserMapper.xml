<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expo.admin.repository.AdminUserMapper">

    <sql id="searchFilter">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (email ILIKE CONCAT('%', #{keyword}, '%')
                     OR nickname ILIKE CONCAT('%', #{keyword}, '%')
                     OR company_name ILIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="accountStatus != null and accountStatus != ''">
                AND account_status = #{accountStatus}
            </if>
        </where>
    </sql>

    <select id="search" resultType="com.expo.admin.dto.AdminUserSummaryResponse">
        SELECT user_id,
               email,
               nickname,
               role,
               account_status,
               company_name,
               created_at,
               last_login_at
        FROM v_admin_users
        <include refid="searchFilter" />
        ORDER BY created_at DESC, user_id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countSearch" resultType="long">
        SELECT count(*)
        FROM v_admin_users
        <include refid="searchFilter" />
    </select>

    <select id="findById" resultType="com.expo.admin.dto.AdminUserDetailResponse">
        SELECT user_id,
               email,
               nickname,
               role,
               account_status,
               phone_number,
               phone_verified_at,
               last_login_at,
               withdrawn_at,
               created_at,
               company_name,
               business_number,
               representative_name,
               business_address,
               business_type,
               business_number_verified,
               business_verified_at,
               verification_provider
        FROM v_admin_users
        WHERE user_id = #{userId}
    </select>

</mapper>
