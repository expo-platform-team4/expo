<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expo.expo.repository.ClientDashboardExpoMapper">

    <select id="findByClientUserId" resultType="com.expo.expo.dto.ClientDashboardExpoResponse">
        SELECT client_user_id,
               expo_id,
               title,
               event_start_at,
               event_end_at,
               sales_start_at,
               sales_end_at,
               review_status,
               visibility_status,
               event_status,
               approved_at,
               ticket_product_count
        FROM v_client_dashboard_expos
        WHERE client_user_id = #{clientUserId}
        ORDER BY event_start_at DESC
    </select>

</mapper>
