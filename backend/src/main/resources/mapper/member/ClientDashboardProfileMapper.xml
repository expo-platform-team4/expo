<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expo.member.repository.ClientDashboardProfileMapper">

    <select id="findByClientUserId" resultType="com.expo.member.dto.ClientDashboardProfileResponse">
        SELECT client_user_id,
               nickname,
               company_name,
               profile_image_file_id,
               profile_image_storage_key,
               profile_image_updated_at
        FROM v_client_dashboard_profile
        WHERE client_user_id = #{clientUserId}
    </select>

</mapper>
