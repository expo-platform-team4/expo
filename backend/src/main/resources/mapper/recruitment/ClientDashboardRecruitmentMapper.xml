<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.expo.recruitment.repository.ClientDashboardRecruitmentMapper">

    <select id="findByHostClientId"
            resultType="com.expo.recruitment.dto.ClientDashboardRecruitmentResponse">
        SELECT host_client_id,
               recruitment_notice_id,
               title,
               status,
               application_start_at,
               application_end_at,
               published_at,
               closed_at,
               submitted_application_count,
               confirmed_allocation_count
        FROM v_client_dashboard_recruitment
        WHERE host_client_id = #{hostClientId}
        ORDER BY application_start_at DESC
    </select>

</mapper>
