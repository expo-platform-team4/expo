package com.expo.banner.entity;

/** 배너 신청 심사 이력의 처리 유형 — banner_review_histories.decision (V1__init_schema 참고). */
public enum BannerReviewDecision {
    SUBMIT,
    APPROVE,
    REJECT,
    CANCEL
}
